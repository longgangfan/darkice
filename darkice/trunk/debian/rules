#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@

override_dh_auto_configure:
	autoreconf -fi
	dh_auto_configure -- \
	  --prefix=/usr \
	  --sysconfdir=/etc \
	  --with-vorbis=yes \
	  --with-flac=yes \
	  --with-alsa=yes \
	  --with-pulseaudio=no \
	  --with-jack=no \
	  --with-samplerate=no
